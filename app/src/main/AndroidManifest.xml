<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.varu.ptactice_attribuits">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_home"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".secondpage" />
        <activity android:name=".Slider_activity" />
        <activity android:name=".cardview" />
        <activity android:name=".Grid">
        </activity>
        <activity android:name=".App1"></activity>
    </application>

</manifest>